<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Welcome to the Platform</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #f4f6f9;
      font-family: 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      color: #333333;
    }
    .wrapper {
      width: 100%;
      padding: 40px 0;
      background-color: #f4f6f9;
    }
    .container {
      max-width: 560px;
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0,0,0,0.08);
    }
    /* Header */
    .header {
      background: linear-gradient(135deg, #405189 0%, #0ab39c 100%);
      padding: 40px 40px 32px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      color: #ffffff;
      font-size: 26px;
      font-weight: 700;
      letter-spacing: -0.5px;
    }
    .header p {
      margin: 8px 0 0;
      color: rgba(255,255,255,0.85);
      font-size: 14px;
    }
    /* Avatar */
    .avatar {
      width: 64px;
      height: 64px;
      border-radius: 50%;
      background: rgba(255,255,255,0.2);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 16px;
      font-size: 28px;
      color: #ffffff;
      font-weight: 700;
      border: 3px solid rgba(255,255,255,0.4);
    }
    /* Body */
    .body {
      padding: 36px 40px;
    }
    .greeting {
      font-size: 18px;
      font-weight: 600;
      margin: 0 0 8px;
      color: #1a1f36;
    }
    .body p {
      margin: 0 0 24px;
      font-size: 14px;
      line-height: 1.7;
      color: #6c757d;
    }
    /* Credentials card */
    .credentials {
      background: #f8f9fc;
      border: 1px solid #e9ebec;
      border-radius: 10px;
      padding: 24px;
      margin: 0 0 28px;
    }
    .credentials h3 {
      margin: 0 0 16px;
      font-size: 13px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      color: #405189;
    }
    .credential-row {
      display: flex;
      align-items: center;
      padding: 10px 0;
      border-bottom: 1px solid #e9ebec;
    }
    .credential-row:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }
    .credential-row:first-of-type {
      padding-top: 0;
    }
    .cred-label {
      width: 90px;
      font-size: 12px;
      font-weight: 600;
      color: #878a99;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      flex-shrink: 0;
    }
    .cred-value {
      font-size: 14px;
      color: #1a1f36;
      font-weight: 500;
      word-break: break-all;
    }
    .cred-value.password {
      font-family: 'Courier New', Courier, monospace;
      background: #e8f4fd;
      color: #0ab39c;
      padding: 3px 10px;
      border-radius: 4px;
      font-size: 14px;
      font-weight: 700;
    }
    /* Warning */
    .warning {
      background: #fff8ec;
      border-left: 4px solid #f7b84b;
      border-radius: 0 8px 8px 0;
      padding: 14px 18px;
      margin: 0 0 28px;
      font-size: 13px;
      color: #856404;
      line-height: 1.6;
    }
    .warning strong {
      display: block;
      margin-bottom: 4px;
    }
    /* CTA button */
    .btn-wrapper {
      text-align: center;
      margin-bottom: 28px;
    }
    .btn {
      display: inline-block;
      background: linear-gradient(135deg, #405189 0%, #0ab39c 100%);
      color: #ffffff !important;
      text-decoration: none;
      padding: 14px 36px;
      border-radius: 8px;
      font-size: 15px;
      font-weight: 600;
      letter-spacing: 0.3px;
    }
    /* Footer */
    .footer {
      border-top: 1px solid #e9ebec;
      padding: 24px 40px;
      text-align: center;
    }
    .footer p {
      margin: 0;
      font-size: 12px;
      color: #adb5bd;
      line-height: 1.6;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="container">

      <!-- Header -->
      <div class="header">
        <div class="avatar">
          <%= (name || 'U').trim().split(/\s+/).map(n => n.charAt(0)).join('').slice(0,2).toUpperCase() %>
        </div>
        <h1>Welcome aboard, <%= name %>!</h1>
        <p>Your account has been created by the admin.</p>
      </div>

      <!-- Body -->
      <div class="body">
        <p class="greeting">Hi <%= name %>,</p>
        <p>
          Your account on our platform has been set up and is ready to use.
          Below are your login credentials. Please keep them safe and do not share
          them with anyone.
        </p>

        <!-- Credentials -->
        <div class="credentials">
          <h3>Your Login Details</h3>
          <div class="credential-row">
            <span class="cred-label">Name</span>
            <span class="cred-value"><%= name %></span>
          </div>
          <div class="credential-row">
            <span class="cred-label">Email</span>
            <span class="cred-value"><%= email %></span>
          </div>
          <div class="credential-row">
            <span class="cred-label">Password</span>
            <span class="cred-value password"><%= password %></span>
          </div>
        </div>

        <!-- Warning -->
        <div class="warning">
          <strong>⚠️ Important — Please read</strong>
          For your security, we strongly recommend changing your password after
          your first login. Never share your password with anyone.
        </div>

        <!-- CTA -->
        <div class="btn-wrapper">
          <a href="#" class="btn">Login to Your Account</a>
        </div>

        <p style="text-align:center; font-size:13px;">
          Having trouble? Reply to this email and we'll help you get started.
        </p>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>
          This is an automated message — please do not reply directly.<br />
          © <%= new Date().getFullYear() %> Admin Panel. All rights reserved.
        </p>
      </div>

    </div>
  </div>
</body>
</html>
